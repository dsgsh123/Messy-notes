1. springmvc在使用get方式请求并使用@PathVariable进行路径参数获取时，mvc会自动将最后一个.及其之后的内容截断丢弃，如url为 main.html时，获取到的参数是main,可以通过参数配置关闭，不是很建议开启这个功能，因为没什么太大用处还有可能导致参数获取出现不正确，使用springmvc的时候是默认开启的，springboot是默认关闭的

2. 使用注解事务时，要确保被注解的方法为public，否则事务无效，原因在于注解事务原理是生成代理类，当被注解的方法访问权限过小，是无法被代理的。