1. 使用毫秒计算日期的时候，一定要注意使用long而不要用int，不然会导致超出范围而计算不准确
   例如：long interval = 120*24*60*60*1000;   这样是不行的，因为虽然最终转化为了long，但表达式的计算结果首先是int型，这个时候已经出现超出范围了，再转化成long是解决不了问题的，正确的做法如下：
   long interval = 120L*24*60*60*1000;   由于使用的是120L而不是120，此时表达式本身计算的结果就是long，所以可以得到正确结果。