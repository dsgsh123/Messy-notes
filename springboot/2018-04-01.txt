1.由于AOP的代理，使得事务注解不能用在private方法上，并且如果类中有a,b两个方法，a中调用了b，b开启事务而a没有，那么事务是不起作用的，只有外部直接调用开启事务的类或者方法，事务才起作用，即内部调用下事务会失效（前提是a没有开启事务）

2. Jpa可以用object数组来接受部分返回值而不用实体类(普通实体类，不对应数据库中的表)

3. hebinate中，利用save进行更新和保存的时候  如果修改了主键（如联合主键）然后save并不会插入一条新纪录，而是会报主键修改错误，想要插入新纪录，可以创建新对象然后把原对象拷贝一份，然后再修改主键，就可以实现复制并插入新纪录。